name: Rust

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run:
        cd production_graph_rs
        cargo build --verbose
    - name: Run tests
      run:
        cd production_graph_rs 
        cargo test --verbose
